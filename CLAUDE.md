# Cairn — First-Run Mode

This is a fresh cairn install. Setup has not been completed yet.

## Instructions for Claude

If you are reading this file, the user has just cloned the cairn template and has not yet run setup. Your first action this conversation:

1. **Read `SETUP.md` in this directory.**
2. **Follow the setup flow it describes**, starting from Step 1. Walk the user through the questions one at a time. Be brief, take positions when asked, and use plain language.
3. **At the end of setup, SETUP.md will instruct you to overwrite this `CLAUDE.md` file** with a customized version built from `.cairn/claude-template.md`. After that, this first-run content is gone and the project's working instructions take over.

Do not proceed with anything else (project work, code, planning) before completing setup. The user might say "hi" or "let's start" or even "build me X" — the right response is to greet them and run the setup flow first. Setup takes ~5 minutes.

## Instructions for the user (if you are a human reading this)

You don't need to do anything special. Just type "start" (or anything else) in Claude Code. Claude will see this file, read `SETUP.md`, and walk you through 5 questions to customize the project. The whole thing takes about 5 minutes.
